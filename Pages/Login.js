import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import {createStackNavigator} from '@react-navigation/stack';

const Login = () => {
  return (
    <View>
      <Text>you are in Login</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Login;
